# Full dependencies for monty_inference.py (x86 Mac/Linux)
#
# Setup:
#   1) pip install --no-deps -e tbp.monty/
#   2) pip install -r scripts/requirements-inference.txt
#   3) python scripts/monty_inference.py
#
# No patching needed — all packages are installed.

# === Core scientific computing ===
numpy<=1.23.5
numpy-quaternion==2023.0.3
scipy>=1.10.1

# === ML / model loading ===
torch>=1.13.1
torch-geometric>=2.1.0    # model.pt unpickle + object_model.py top-level import
torch-scatter>=2.0.9
torch-sparse>=0.6.15

# === Image processing ===
pillow
scikit-image
scikit-learn==1.3.2
matplotlib>=3.7.3

# === Transitive deps (tbp.monty internal imports) ===
omegaconf>=2.3.0          # buffer.py
pydantic>=2.10.6          # actions.py
eval_type_backport        # pydantic needs this on python <3.10
pandas                    # graph_matching_loggers.py
wandb                     # graph_matching_loggers.py
typing-extensions
tqdm
